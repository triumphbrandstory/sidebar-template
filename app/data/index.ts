import "server-only";

import { memories } from "./memories";
import { user_preferences } from "./user-preferences";
import { users } from "./users";

export const data = {
  memories,
  users,
  user_preferences,
};
